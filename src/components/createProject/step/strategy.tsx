import { IStepBaseProps } from '@/components/createProject/step/start';

export interface IStepStrategyProps extends IStepBaseProps {}

export default function StepStrategy(props: IStepStrategyProps) {
	return <>StepStrategy</>;
}
